export interface Credentials {
  email: string;
  password: string;
}

export interface fetchData {
  avatar: string;
  email: string;
  first_name: string;
  id: number;
  last_name: string;
}

export interface AddUser {
  name: string;
  job: string;
}